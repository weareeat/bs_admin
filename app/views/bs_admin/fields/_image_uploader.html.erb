<input
  class="imageUploadWrapper"
  type="file"
  name="<%= name %>"
  <%= multiple ? "multiple" : "" %>
  get-all-url="<%= all_url %>"
  destroy-url="<%= destroy_url %>"
  data-url="<%= upload_url %>"
  data-group="all" />